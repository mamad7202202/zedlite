[package]
name = "zedlite"
version = "0.1.0"
edition = "2021"
description = "A tiny Zed-inspired code editor built with GPUI"
license = "Apache-2.0"

[dependencies]
gpui = "0.2.2"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
